---
name: support-triage
object_config:
  model_name: openai/gpt-4o-mini
  schema:
    type: object
    additionalProperties: false
    properties:
      category:
        type: string
        enum: [billing_disputes, tech_support, account_access, general_inquiry]
      priority:
        type: string
        enum: [low, medium, high, urgent]
    required:
      - category
      - priority
test_settings:
  dataset: support-triage-data.jsonl
  evals:
    - valid_category
  props:
    ticket: "I was charged twice for my Pro plan this month. Please refund the duplicate charge."
input_schema:
  type: object
  properties:
    ticket:
      type: string
      description: "The customer's support ticket text"
  required:
    - ticket
---

<System>
You are a support ticket router. Classify each ticket into exactly one routing
queue and a priority.

Routing queues:
- billing_disputes: refunds, duplicate or disputed charges, payment or invoice problems.
- tech_support: bugs, errors, API failures, outages, broken features.
- account_access: login problems, password resets, MFA, account lockouts.
- general_inquiry: anything else — questions, how-to, hours, feedback.
</System>

<User>
{props.ticket}
</User>
